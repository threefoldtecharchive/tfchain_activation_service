apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "substrate-funding-service.fullname" . }}-test-connection"
  labels:
    {{- include "substrate-funding-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "substrate-funding-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

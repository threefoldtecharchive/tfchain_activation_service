apiVersion: v1
kind: Service
metadata:
  name: {{ include "substrate-funding-service.fullname" . }}
  labels:
    {{- include "substrate-funding-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "substrate-funding-service.selectorLabels" . | nindent 4 }}
